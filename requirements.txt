keras>=3.0
# Keras 3's numpy backend imports scipy + jax at startup even though we
# don't run anything on them — they're required just to import keras.
scipy>=1.10
jax>=0.4
jaxlib>=0.4
numpy>=1.24
pygame>=2.5
matplotlib>=3.7
imageio>=2.31
imageio-ffmpeg>=0.4.9
playwright>=1.40
